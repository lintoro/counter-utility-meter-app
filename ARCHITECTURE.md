# 系統架構與業務規格 (System Architecture & Business Spec)

## 一、 核心概念：解耦的雙表設計

為保護既有財務計費主表結構不被照片或暫存狀態污染，本系統採用雙表分離架構：

1. **`水電軌道燈紀錄_Log`（計費主檔 - 最終 SSOT，亦相容 `def_水電軌道燈紀錄_Log`）**：
   - 僅接受最終合格審核後的度數寫入，不存放照片欄位。
   - 欄位結構（A 到 P 欄）：
     - `A: 結帳年月` (例如 202607)
     - `B: 專櫃代碼` (例如 00009)
     - `C: 專櫃名稱` (例如 瑪莎拉)
     - `D: 前期抄表日`
     - `E: 本期抄表日` (寫入點 YYYY/M/D)
     - `F: 220V電費前期度數`
     - `G: 220V電費本期度數` (寫入點)
     - `H: 110V電費前期度數`
     - `I: 110V電費本期度數` (寫入點)
     - `J: 水費前期度數`
     - `K: 水費本期度數` (寫入點)
     - `L~P: 營業參數與單價`

2. **`抄表待審核_Queue`（暫存核對表 - AppSheet 操作對象）**：
   - 供 AppSheet 呈現場景卡片，儲存照片二進位與 AI 辨識中間狀態。
   - 欄位規劃：
     - `ID`（唯一的 Record ID）
     - `上傳時間`、`照片`（Image URL）
     - `專櫃代碼`（Ref 關聯至櫃位主檔）
     - `專櫃名稱`、`儀表類別` (110V/220V/水表)
     - `前期度數`（自動帶出）
     - `AI辨識度數`（Gemini 提取之初值）
     - `人工覆核度數`（同仁手動修正欄位）
     - `本期用量`（覆核度數 - 前期度數）
     - `審核狀態`（待覆核 / 正常 / 異常 / 已過帳）
     - `審核備註`（例如：拍到電壓畫面、度數逆轉等）

---

## 二、 四步驟操作動線與 AppSheet 介面設計

```text
[AppSheet 首頁]
 ├── 按鈕 1: 【📷 批次上傳照片】 (Deep Link 喚起 Google Drive 資料夾 -> 全選上傳 -> 返回 App)
 ├── 按鈕 2: 【🤖 開始 AI 辨識】 (觸發 GAS 呼叫 Gemini 2.0 Flash 提取並填入 Queue)
 ├── 清單區: 【待審核卡片清單 (Deck View)】
 │    ├── 綠色卡片：辨識成功且用量合理 (免動)
 │    └── 紅色卡片：模糊或跳電壓 (點開卡片看照片 -> 輸入數字 -> 儲存)
 └── 按鈕 3: 【✅ 一鍵確認過帳】 (將合格度數與抄表日批次回填至 def_水電軌道燈紀錄_Log)
```

---

## 三、 儀表專業辨識與防呆規則 (Meter Reading Rules)

1. **110V 機械電表 (大同 E-31)**：
   - 黑色字輪為整數，最右側**紅色字輪框為小數點後第 1 位 (0.1)**，四捨五入取整數。
   - 字輪若在交界處（半字），以較小整數為準。
2. **220V 集合式數位儀表 (士林電機 SPM-3)**：
   - **單位防呆**：畫面必須有 `kWh` 字樣。若顯示為 `V` (電壓)、`A` (電流)、`kW` (功率)，判定為無效輪播畫面，標註紅燈。
3. **水表 (機械水量計 KC-20C)**：
   - 僅讀取上方**黑色字輪**（立方公尺整數度數）。下方紅色指針（小數）一律忽略。
4. **度數逆轉防呆**：
   - 若本期度數 < 前期度數（非合理歸零），標註紅燈，禁止過帳。
